/* global module: true, require: true, console: true */
"use strict";

var should = require('chai').should(),
  Nemo = require('../index');

describe("@nemoData@ suite", function () {
  describe("@environmentVariable@", function () {

    it("should launch a local server", function (done) {

    });
  });
  describe("@jsonVariable@", function () {
    it("should launch a local server", function (done) {

    });
    it("should override any nemoData environment variable", function (done) {

    });
  });
});
